<%= form_for(@agenda) do |f| %>
  <% if @agenda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda.errors.count, "error") %> prohibited this agenda from being saved:</h2>

      <ul>
      <% @agenda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tarea %><br>
    <%= f.text_field :tarea %>
  </div>
  <div class="field">
    <%= f.label :prioridad %><br>
    <%= f.number_field :prioridad %>
  </div>
  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :descripccion %><br>
    <%= f.text_area :descripccion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
